function [ld] = updateVidsetHasCal(ld)
%updateVidsetHasCal checks the database to see if a vidset has a
%corresponding desinusoid matrix


% cal_fovs = 
for ii=1:numel(ld.vid.vid_set)
    fov = lid.vid.vid_set(ii).fov;
    
    
    
end

end

